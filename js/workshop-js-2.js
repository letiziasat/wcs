/*
Create a function that receive a number between 0 and 3
if that number is 0 return an empty array
if that number is 1 return an array with 1 elements
if that number is 2 return an array with 2 elements;
At the end log what your function returns; */

function num(n) {
    
    if (n === 0){
    return [];
    }
    if (n === 1){
    return [1];
    }
    if (n === 2){
    return [1, 2];
    }
    }
    